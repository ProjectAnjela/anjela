name: Deploy Anjela

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Check repository
        run: |
          echo "Anjela deployment workflow started"
          echo "Repository: ${{ github.repository }}"
          echo "Branch: ${{ github.ref_name }}"
